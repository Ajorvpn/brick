# iOS Native Module

This directory is reserved for the future iOS VPN integration. A later phase
will populate it with the Swift `NetworkExtension` implementation and its
platform-channel boundary, following the future iOS strategy described in
[ARCHITECTURE.md](../../AI_ROLES/ARCHITECTURE.md), Section 3.4.

This Phase 0 scaffold intentionally contains no Xcode project, Swift source,
Network Extension, or iOS build configuration.
