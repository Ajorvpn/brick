name: core_vpn_engine
description: "Brick VPN core VPN engine abstractions (pure Dart, no Flutter dependency)"
publish_to: 'none'
resolution: workspace
environment:
  sdk: ^3.13.3
dependencies:
  core_domain: 'any'
dev_dependencies:
  test: ^1.31.1
